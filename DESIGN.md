# DESIGN.md — TATA 官網重新設計提案

## 設計定位

深藍＋金的機構感。參考座標：IATA / WTTC / 國家級觀光局的年報與官網——端莊、克制、經得起理監事會投影檢視。TATA 既有識別為藍色，本案是 identity-preserving 的升級，不是重新品牌化。

## Color（OKLCH 定義，附 hex 參考）

- `--navy-950` oklch(0.20 0.045 258) / `#081527` — 深色段底、footer
- `--navy-900` oklch(0.26 0.055 258) / `#0D2440` — 主品牌色、hero 疊層、按鈕
- `--navy-700` oklch(0.36 0.06 258) / `#1B3A5C` — hover、次要深底
- `--gold-500` oklch(0.70 0.10 88) / `#C2A356` — 唯一的強調色：細線、數字、hover 下線、小標。不大面積填充
- `--gold-600` oklch(0.62 0.10 88) / `#A98A45` — 金在深底上的文字用色（對比修正）
- `--paper` oklch(0.975 0.004 255) / `#F7F8FA` — 淺色段底（冷調白，禁止米色/奶油底）
- `--ink` oklch(0.28 0.04 258) / `#14263F` — 淺底正文標題
- `--body` oklch(0.38 0.03 258) / `#33465E` — 正文（對比 ≥ 7:1）
- `--muted` oklch(0.50 0.025 258) / `#5B6B7F` — 輔助說明（只用於大號字或 ≥4.5:1 場合）
- `--line` oklch(0.90 0.008 255) / `#DCE1E8` — 分隔細線

規則：全站只有「藍、金、灰階」三族。對手版的綠/橘/粉/紫一律消滅。CTA 按鈕只有兩種：navy 實底、或深底上的描邊金字。

## Typography

- 顯示字：**Noto Serif TC**（600/700）——中文標題的書卷氣是本案靈魂。英文小標同用 serif 或 letterspaced sans。
- 正文/UI：**Noto Sans TC**（400/500/700）。
- 規模（fluid clamp）：hero `clamp(2.5rem, 5vw, 4.5rem)`；段標題 `clamp(1.75rem, 3vw, 2.75rem)`；letter-spacing 中文 0.02em、顯示字不低於 -0.04em。
- 正文行長 ≤ 65ch，行高 1.8（中文）；`text-wrap: balance` 用於 h1–h3。
- **禁止**每個段落都放英文小膠囊 eyebrow（對手版就是這樣露餡）。段落識別改用：細金線＋段標題，或直排中文小標（書卷氣的差異化手法，全站最多用 2–3 次）。
- 數字用 tabular-nums；西元年「1969」不做 count-up、不加千分位。

## Layout

- 最大寬 1200px，但 hero 與部分影像 full-bleed；段落留白 `clamp(5rem, 10vw, 9rem)` 上下，節奏要有變化，不要每段等高。
- **禁止一模一樣的卡片海**。各段版式必須不同：
  - 核心服務（8 項）→ 編號索引列表（大編號＋標題＋一行說明＋細線分隔），hover 時編號轉金。
  - 最新消息 → 報紙式：左欄 1 則頭條（大圖），右欄 5 則純文字列表（日期＋分類＋標題＋細線）。
  - 活動專區 → 深藍段，橫式列（日期方塊｜標題＋地點｜報名進度條｜CTA），進度條金線。
  - 合作夥伴 → 名字用 serif 排版的文字 marquee（對手用假 logo 色塊；我們用字體排版反而高級），hover 暫停。
- 卡片圓角 ≤ 12px；禁止「1px 邊框＋大模糊陰影」同時出現；分隔主要靠細線與留白。
- 非對稱：about 段圖文 5:7 錯位；理事長引言用大號 serif 引文，不用卡片。

## Imagery

- 全部用真實照片（Wikimedia Commons 下載到 `assets/img/`，下載後必須用眼睛確認內容正確、無簡體浮水印）。需要：台北天際線/101 黃昏（hero）、旅展或會展場景、機場/飛機、會議/論壇、podcast 麥克風。
- Hero：全幅照片＋navy-950 漸層疊層（上淺下深，確保標題對比 ≥ 4.5:1）。
- 圖片統一輕微降飽和＋冷色調（CSS filter），避免圖庫照片各自為政。
- 裝飾性 AI 生圖、手繪 SVG 插畫：禁止。

## Motion（GSAP + ScrollTrigger CDN）

- Hero 進場：標題分行上浮＋疊層淡入，一次到位的編排（1.2s 內），ease-out-expo。
- 統計數字：只有「會員數/委員會/活動」做 count-up；「1969」是年份，直接顯示。
- 活動倒數：用真實日期計算「距離 X 天」，不是裝飾。
- 段落 reveal：內容**預設可見**，動畫只是 enhance（對手版在無 JS/截圖時整頁空白就是死在這）。
- `prefers-reduced-motion`：全部改為立即顯示。
- 不動 layout 屬性；無 bounce/elastic。

## 內容規矩

- 一律繁體中文（台灣用語），交付前全文檢查無簡體字。
- 不用裝飾性 emoji；icon 用細線 SVG（heroicons/lucide 風格，統一 1.5px 筆畫）。
- Footer 需註明「本頁為設計提案示範（Redesign Concept）」。
